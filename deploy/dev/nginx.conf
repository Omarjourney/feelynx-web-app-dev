events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  # WebSocket upgrade support
  map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
  }

  # =====================================================
  # Feelynx Nginx Config — Dev/Prod Ready
  # =====================================================
  server {
    listen 80;
    server_name _;

    # ------------------------------
    # Serve Frontend (Vite/Next.js)
    # ------------------------------
    root /usr/share/nginx/html;
    index index.html;

    # Don't cache HTML files - always fetch fresh
    location ~* \.html$ {
      expires -1;
      add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
    }

    # Cache static assets aggressively (images, fonts, JS, CSS)
    location ~* \.(?:js|css|png|jpg|jpeg|webp|avif|svg|gif|woff2|woff|ttf)$ {
      expires 30d;
      add_header Cache-Control "public, max-age=2592000, immutable";
      access_log off;
    }

    # Fallback for assets path (if used by build) - keep for compatibility
    location /assets/ {
      expires 30d;
      add_header Cache-Control "public, immutable";
      access_log off;
    }

    # ------------------------------
    # Proxy to Backend API
    # ------------------------------
    location /api/ {
      proxy_pass http://backend:8000/;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ------------------------------
    # WebSocket Support for /ws/
    # ------------------------------
    location /ws/ {
      proxy_pass http://backend:8000/ws/;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_read_timeout 600s;
      proxy_send_timeout 600s;
    }

    # ------------------------------
    # Socket.io / WebSocket Upgrade Support
    # ------------------------------
    location /socket.io/ {
      proxy_pass http://backend:8000;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_read_timeout 600s;
      proxy_send_timeout 600s;
    }

    # ------------------------------
    # Health Check Endpoint
    # ------------------------------
    location /health {
      proxy_pass http://backend:8000/health;
    }

    # ------------------------------
    # SPA Fallback (for React/Vite)
    # ------------------------------
    location / {
      try_files $uri /index.html;
    }
  }
}
